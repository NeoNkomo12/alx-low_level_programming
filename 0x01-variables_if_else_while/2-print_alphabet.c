#include <stdlib.h>
#include <time.h>
#include<stdio.h>

/**
 * main - Access point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char[26]="abcdefghijkllmnopqrstuvwxyz", int i;

	for (i = 0; i < 26, i++)
	{
		putchar(alph[i]);
	}
	putchar("\n");
	return(0);
}
